# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login/login.spec.js >> Kaleyra Login Module >> Invalid Password Test
- Location: tests/login/login.spec.js:28:5

# Error details

```
"beforeAll" hook timeout of 30000ms exceeded.
```

```
Error: page.goto: Target page, context or browser has been closed
Call log:
  - navigating to "https://adminqa.ecosmob.net:3006/login", waiting until "load"

```

# Test source

```ts
  1   | const { test, expect } = require('@playwright/test');
  2   | 
  3   | const LoginPage = require('../../pages/LoginPage');
  4   | 
  5   | const testData = require('../../utils/testData');
  6   | 
  7   | test.describe.serial('Kaleyra Login Module', () => {
  8   | 
  9   |     let page;
  10  |     let loginPage;
  11  | 
  12  |     test.beforeAll(async ({ browser }) => {
  13  | 
  14  |         page = await browser.newPage();
  15  | 
> 16  |         await page.goto(testData.loginUrl);
      |                    ^ Error: page.goto: Target page, context or browser has been closed
  17  | 
  18  |         await page.waitForTimeout(3000);
  19  | 
  20  |         loginPage = new LoginPage(page);
  21  |     });
  22  | 
  23  |     test.afterAll(async () => {
  24  | 
  25  |         await page.close();
  26  |     });
  27  | 
  28  |     test('Invalid Password Test', async () => {
  29  | 
  30  |         await loginPage.emailInput.clear();
  31  |         await loginPage.passwordInput.clear();
  32  | 
  33  |         await loginPage.login(
  34  |             testData.validEmail,
  35  |             testData.invalidPassword
  36  |         );
  37  | 
  38  |         await page.waitForTimeout(3000);
  39  | 
  40  |         await expect(page)
  41  |             .toHaveURL(/login/);
  42  |     });
  43  | 
  44  |     test('Invalid Email Test', async () => {
  45  | 
  46  |         await loginPage.emailInput.clear();
  47  |         await loginPage.passwordInput.clear();
  48  | 
  49  |         await loginPage.login(
  50  |             testData.invalidEmail,
  51  |             testData.validPassword
  52  |         );
  53  | 
  54  |         await page.waitForTimeout(3000);
  55  | 
  56  |         await expect(page)
  57  |             .toHaveURL(/login/);
  58  |     });
  59  | 
  60  |     test('Blank Email Validation Test', async () => {
  61  | 
  62  |         await loginPage.emailInput.clear();
  63  |         await loginPage.passwordInput.clear();
  64  | 
  65  |         await loginPage.passwordInput
  66  |             .fill(testData.validPassword);
  67  | 
  68  |         await loginPage.signInButton.click();
  69  | 
  70  |         await page.waitForTimeout(3000);
  71  | 
  72  |         await expect(page)
  73  |             .toHaveURL(/login/);
  74  |     });
  75  | 
  76  |     test('Blank Password Validation Test', async () => {
  77  | 
  78  |         await loginPage.emailInput.clear();
  79  |         await loginPage.passwordInput.clear();
  80  | 
  81  |         await loginPage.emailInput
  82  |             .fill(testData.validEmail);
  83  | 
  84  |         await loginPage.signInButton.click();
  85  | 
  86  |         await page.waitForTimeout(3000);
  87  | 
  88  |         await expect(page)
  89  |             .toHaveURL(/login/);
  90  |     });
  91  | 
  92  |     test('Valid Login Test', async () => {
  93  | 
  94  |         await loginPage.emailInput.clear();
  95  |         await loginPage.passwordInput.clear();
  96  | 
  97  |         await loginPage.login(
  98  |             testData.validEmail,
  99  |             testData.validPassword
  100 |         );
  101 | 
  102 |         await page.waitForTimeout(5000);
  103 | 
  104 |         await expect(page)
  105 |             .not.toHaveURL(/login/);
  106 |     });
  107 | 
  108 | });
```